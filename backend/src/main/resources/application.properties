# Server Configuration
server.port=${PORT:8080}

# Database Configuration - MySQL (Supports Cloud Env Vars & Local Fallback Defaults)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/stringstacks_ecommerce?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Swathi%8639580546}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
toyland.app.jwtSecret=${JWT_SECRET:ToylandSecretKeyForJwtAuthenticationSystemMustBeAtLeast32BytesLong1234567890}
toyland.app.jwtExpirationMs=86400000

# Razorpay Test Mode Configuration
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_TdrNwXVXQOMj7j}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:dummy_test_secret_for_signature}
razorpay.shipping.fee=50.00

# Mail Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}

app.frontend.url=${FRONTEND_URL:http://localhost:5173}